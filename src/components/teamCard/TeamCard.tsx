import styles from './teamCard.module.css';

// TYPES
import { TeamCardProps } from './teamCard.interface'

export default function TeamCard({}: TeamCardProps) {
    return (
        <div>
            TeamCard
        </div>
    );
};
